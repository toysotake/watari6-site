(()=>{let canvas,ctx,raf,pieces=[];const colors=['#ffd23f','#ff4d6d','#6ee7ff','#8b5cf6','#4ade80','#fff'];function ensure(){if(canvas)return;canvas=document.createElement('canvas');canvas.setAttribute('aria-hidden','true');Object.assign(canvas.style,{position:'fixed',inset:'0',width:'100%',height:'100%',pointerEvents:'none',zIndex:'100'});document.body.appendChild(canvas);ctx=canvas.getContext('2d');resize();addEventListener('resize',resize)}function resize(){if(!canvas)return;const d=Math.min(devicePixelRatio||1,2);canvas.width=innerWidth*d;canvas.height=innerHeight*d;ctx.setTransform(d,0,0,d,0,0)}function burst(){if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;ensure();cancelAnimationFrame(raf);pieces=Array.from({length:90},(_,i)=>{const side=i%2?1:-1;return{x:side>0?innerWidth+10:-10,y:innerHeight*.45+Math.random()*80,vx:side*(-(4+Math.random()*8)),vy:-(5+Math.random()*10),g:.22+Math.random()*.12,r:Math.random()*Math.PI,w:5+Math.random()*7,h:3+Math.random()*5,c:colors[i%colors.length],life:0}});tick()}function tick(){ctx.clearRect(0,0,innerWidth,innerHeight);pieces.forEach(p=>{p.x+=p.vx;p.y+=p.vy;p.vy+=p.g;p.r+=.16;p.life++;ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.r);ctx.fillStyle=p.c;ctx.fillRect(-p.w/2,-p.h/2,p.w,p.h);ctx.restore()});pieces=pieces.filter(p=>p.life<150&&p.y<innerHeight+30);if(pieces.length)raf=requestAnimationFrame(tick);else ctx.clearRect(0,0,innerWidth,innerHeight)}window.naniutuConfetti=burst})();